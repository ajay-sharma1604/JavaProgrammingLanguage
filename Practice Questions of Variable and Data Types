Question1:In a program,input 3 numbers:A,BandC.You have to output the average of these 3 numbers.(Hint : Average of N numbers is sum of those numbers divided by N)

import java.util.*;
public class Basics{
    public static void main(String args[]){
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter A");
        int A = sc.nextInt();
        System.out.println("Enter B");
        int B = sc.nextInt();
        System.out.println("Enter C");
        int C = sc.nextInt();
        int Avg = (A+B+C)/3;
        System.out.println("Hence the required Average is " + Avg);
        //System.out.println(Avg);
    }
}

Question2:In a program,input the side of a square.You have to output the area of the square.(Hint : area of a square is (side x side))

import java.util.*;
public class Basics{
    public static void main(String args[]){
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter side of a square");
        int Side = sc.nextInt();
        int Area= Side*Side;
        System.out.println("Hence the Area of Square is: " + Area);
        //System.out.println(Area);
    }
}

Question3:Enter cost of 3 items from the user(using float data type)-a pencil,a pen and an eraser. You have to output the total cost of the items back to the user as their bill.(Add on : You can also try adding 18% gst tax to the items in the bill as an advanced problem)

import java.util.*;
public class Basics{
    public static void main(String args[]){
        Scanner sc = new Scanner(System.in);
        System.out.println("ENTER COST OF 1st ITEM- Pencil");
        float item1 = sc.nextFloat();
        System.out.println("ENTER COST OF 2nd ITEM- Pen");
        float item2 = sc.nextFloat();
        System.out.println("ENTER COST OF 3rd ITEM- Eraser");
        float item3 = sc.nextFloat();
        float total = item1+ item2 + item3;
         System.out.println("Bill:- " + total);
        //System.out.println(total);

        //Adding 18% gst tax to the bill
         float newtotal = total + (total*0.18f);
         System.out.println("Bill with 18% GST tax: " + newtotal);

    }
}

Question 4: What will be the type of result in thefollowing Java code?

(Hint : Look at the largest data type among these)

        byte b = 4;
        char c = 'a';
        short s = 512;
        int i = 1000;
        float f = 3.14f;
        double d = 99.9954;
        result = (f*b)+(i % c) -(d*s);

ANSWER- Double

import java.util.*;
public class Basics{
    public static void main(String args[]){
        Scanner sc = new Scanner(System.in);
        byte b = 4;
        char c = 'a';
        short s = 512;
        int i = 1000;
        float f = 3.14f;
        double d = 99.9954;

        Double result = (f*b)+(i % c) -(d*s);
        System.out.println(result);

    }
}


Question 5:(Advanced)Will the following statement give any error in Java? 
                             int$=24;

answer:- No, the statement will not give any error.
Names of variables are called identifiers inJava.
Identifier rule says,identifiers can start with any alphabet or underscore (“_”) or dollar (“$”).
According to the rule the given variable name is a valid identifier.

